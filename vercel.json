{
    "builds": [{
        "src": "weather_forecast/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "weather_forecast/wsgi.py"
        }
    ]
}